spring.application.name=medistore

# Server port
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${DB_URL}?sslmode=require
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
app.jwt.secret=${APP_JWT_SECRET}
app.jwt.expiration=86400000

# Reset password
app.reset-token.expiration=1800 

# Email to reset password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your_email@gmail.com 
spring.mail.password=your_app_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend link for password reset
app.reset-password.url=http://localhost:3000/reset-password?token=

# =========================
# VNPAY
# =========================

vnpay.tmn-code=US14NBJU

vnpay.hash-secret=K3K6NCMUCM1LS7I8FV3Y2NQ3PEU9JJOZ

vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# Frontend result page
vnpay.return-url=https://medi-store-frontend-two.vercel.app/payment-result

# Backend IPN callback
vnpay.ipn-url=https://YOUR_REAL_NGROK_URL.ngrok-free.app/api/payments/vnpay-ipn